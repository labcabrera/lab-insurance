spring:
  profiles:
    active: cloud

---

spring:
  profiles: cloud
  cloud:
    config:
      uri: http://localhost:8888/
      name: insurance-portfolio-core,insurance-common

---

spring:
  profiles: local
  cloud:
    config:
      enabled: false
