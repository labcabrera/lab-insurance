spring:
  profiles:
    active: test

---

spring:
  profiles: cloud
  cloud:
    config:
      uri: http://localhost:8888/
      name: insurance-contract-creation-core,insurance-common

---

spring:
  profiles: test
  cloud:
    config:
      enabled: false
