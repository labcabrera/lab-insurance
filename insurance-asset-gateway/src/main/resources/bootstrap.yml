spring:
  profiles:
    active: cloud

---

spring:
  profiles: cloud
  cloud:
    config:
      uri: http://localhost:8888/
      name: insurance-asset-gateway,insurance-common

---

spring:
  profiles: local
  cloud:
    config:
      enabled: false
