spring:
  profiles:
    active: local

---

spring:
  profiles: cloud
  cloud:
    config:
      uri: http://localhost:8888/
      name: insurance-asset-core,insurance-common

---

spring:
  profiles: local
  cloud:
    config:
      enabled: false
